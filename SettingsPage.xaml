<Page
    x:Class="SteamRipApp.SettingsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SteamRipApp"
    xmlns:core="using:SteamRipApp.Core"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <ScrollViewer Padding="24">
        <StackPanel Spacing="32" MaxWidth="800" HorizontalAlignment="Left">

            <StackPanel Spacing="8">
                <TextBlock Text="⬇️ Default Download Directory" Style="{StaticResource TitleTextBlockStyle}"/>
                <TextBlock Text="Games will download here automatically without prompting each time." Style="{StaticResource CaptionTextBlockStyle}"/>
                <Grid Padding="12" Background="{ThemeResource SystemControlBackgroundChromeMediumLowBrush}" CornerRadius="4" Margin="0,8,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock x:Name="DownloadDirLabel" Text="Not set — you will be prompted on first download."
                               VerticalAlignment="Center" TextTrimming="CharacterEllipsis"/>
                    <Button Grid.Column="1" Content="Change Directory" Margin="12,0,0,0"
                            Style="{StaticResource AccentButtonStyle}" Click="ChangeDownloadDir_Click"/>
                </Grid>
            </StackPanel>

            <Rectangle Height="1" Fill="{ThemeResource SystemControlBackgroundBaseLowBrush}" Margin="0,8"/>

            <StackPanel Spacing="8">
                <TextBlock Text="📁 Scan Directories" Style="{StaticResource TitleTextBlockStyle}"/>
                <TextBlock Text="Locations where your pre-installed SteamRip games are located." Style="{StaticResource CaptionTextBlockStyle}"/>

                <ItemsControl x:Name="DirList" Margin="0,12,0,12">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Grid Margin="0,4" Padding="12" Background="{ThemeResource SystemControlBackgroundChromeMediumLowBrush}" CornerRadius="4">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="{Binding}" VerticalAlignment="Center"/>
                                <Button Grid.Column="1" Content="✕" Click="RemoveDir_Click" Tag="{Binding}" Background="Transparent" Foreground="Red" BorderThickness="0"/>
                            </Grid>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

                <Button Content="Add Directory" Click="AddDir_Click" Style="{StaticResource AccentButtonStyle}"/>
            </StackPanel>

            <Rectangle Height="1" Fill="{ThemeResource SystemControlBackgroundBaseLowBrush}" Margin="0,8"/>

            <Rectangle Height="1" Fill="{ThemeResource SystemControlBackgroundBaseLowBrush}" Margin="0,8"/>

            <StackPanel Spacing="16">
                <TextBlock Text="🛠 Library Maintenance" Style="{StaticResource TitleTextBlockStyle}"/>
                <Button Content="REPAIR &amp; SYNC LIBRARY" Click="RepairSync_Click" Width="240" Height="48" Style="{StaticResource AccentButtonStyle}"/>
                <TextBlock Text="Scans all local directories and updates the metadata bank. This may take a minute." Style="{StaticResource CaptionTextBlockStyle}"/>

                <Button Content="RESET TO INITIAL SETUP" Click="SetupBtn_Click" Width="240" Height="40" Margin="0,12,0,0"/>
                <TextBlock Text="Resets settings and preferences. Your library and downloaded games will not be deleted." Style="{StaticResource CaptionTextBlockStyle}"/>
            </StackPanel>

            <Rectangle Height="1" Fill="{ThemeResource SystemControlBackgroundBaseLowBrush}" Margin="0,8"/>

            <StackPanel Spacing="16">
                <TextBlock Text="📦 Downloads &amp; Extraction" Style="{StaticResource TitleTextBlockStyle}"/>

                <StackPanel Spacing="8">
                    <TextBlock Text="Speed Units"/>
                    <StackPanel Orientation="Horizontal" Spacing="20">
                        <RadioButton x:Name="SpeedBitsRadio" Content="Bits (Mbps)" Checked="SpeedUnit_Checked" GroupName="SpeedUnitGroup"/>
                        <RadioButton x:Name="SpeedBytesRadio" Content="Bytes (MB/s)" Checked="SpeedUnit_Checked" GroupName="SpeedUnitGroup"/>
                    </StackPanel>
                </StackPanel>

                <StackPanel Spacing="12">
                    <TextBlock Text="Preferred Extraction Method"/>

                    <Grid Padding="12" Background="{ThemeResource SystemControlBackgroundChromeMediumLowBrush}" CornerRadius="4">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <RadioButton x:Name="ExtractUnRarDLLRadio" Content="High-Precision Engine (Internal)" VerticalAlignment="Center" Checked="ExtractMethod_Checked" Tag="UnRarDLL" GroupName="ExtractMethodGroup"/>
                        <TextBlock x:Name="UnRarDllStatus" Grid.Column="1" Text="Ready" VerticalAlignment="Center" Margin="20,0,0,0" FontSize="11" Opacity="0.7"/>
                        <Border Grid.Column="2" Background="{ThemeResource SystemControlBackgroundBaseLowBrush}" CornerRadius="4" Padding="8,4">
                            <TextBlock Text="Bundled" FontSize="10" FontWeight="Bold" Opacity="0.8"/>
                        </Border>
                    </Grid>

                    <Grid Padding="12" Background="{ThemeResource SystemControlBackgroundChromeMediumLowBrush}" CornerRadius="4">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <RadioButton x:Name="ExtractWinRarRadio" Content="WinRAR (Recommended)" VerticalAlignment="Center" Checked="ExtractMethod_Checked" Tag="WinRAR" GroupName="ExtractMethodGroup"/>
                        <TextBlock x:Name="WinRarStatus" Grid.Column="1" Text="Detecting..." VerticalAlignment="Center" Margin="20,0,0,0" FontSize="11" Opacity="0.7"/>
                        <Button Grid.Column="2" Content="Download" Click="DownloadWinRar_Click" Style="{StaticResource AccentButtonStyle}" FontSize="11"/>
                    </Grid>

                    <Grid Padding="12" Background="{ThemeResource SystemControlBackgroundChromeMediumLowBrush}" CornerRadius="4">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <RadioButton x:Name="ExtractSevenZipRadio" Content="7-Zip (Recommended)" VerticalAlignment="Center" Checked="ExtractMethod_Checked" Tag="SevenZip" GroupName="ExtractMethodGroup"/>
                        <TextBlock x:Name="SevenZipStatus" Grid.Column="1" Text="Detecting..." VerticalAlignment="Center" Margin="20,0,0,0" FontSize="11" Opacity="0.7"/>
                        <Button Grid.Column="2" Content="Download" Click="DownloadSevenZip_Click" Style="{StaticResource AccentButtonStyle}" FontSize="11"/>
                    </Grid>

                    <Grid Padding="12" Background="{ThemeResource SystemControlBackgroundChromeMediumLowBrush}" CornerRadius="4">
                        <RadioButton x:Name="ExtractWindowsRadio" Content="Windows Standard (Not Recommended - Slow)" VerticalAlignment="Center" Checked="ExtractMethod_Checked" Tag="Windows" GroupName="ExtractMethodGroup"/>
                    </Grid>
                </StackPanel>
            </StackPanel>

            <Rectangle Height="1" Fill="{ThemeResource SystemControlBackgroundBaseLowBrush}" Margin="0,8"/>

            <StackPanel Spacing="16">
                <TextBlock Text="🧪 Development" Style="{StaticResource TitleTextBlockStyle}"/>
                <StackPanel Spacing="8">
                    <ToggleSwitch x:Name="AdvancedModeToggle" Header="Developer Mode"
                                  Toggled="AdvancedModeToggle_Toggled"
                                  OnContent="Enabled" OffContent="Disabled"/>
                    <ToggleSwitch x:Name="HardRepairToggle" Header="Deep Integrity Scan"
                                  Toggled="HardRepairToggle_Toggled"
                                  OnContent="Enabled" OffContent="Disabled"/>
                    <TextBlock Text="Verifies every byte of every file for corruption. Slower but more thorough." FontSize="11" Opacity="0.7" Margin="0,-8,0,12"/>
                    <ToggleSwitch x:Name="MultiThreadedHashingToggle" Header="Multi-threaded Hashing (Faster but high CPU)"
                                  Toggled="MultiThreadedHashingToggle_Toggled"
                                  OnContent="Enabled" OffContent="Disabled"/>

                    <TextBlock Text="Generates and stores file integrity maps. Allows for targeted restoration of corrupted files without full re-downloads." FontSize="11" Opacity="0.7" Margin="0,-8,0,12"/>

                    <ToggleSwitch x:Name="SteamIntegrationToggle" Header="Show Steam Tab"
                                  Toggled="SteamIntegrationToggle_Toggled"
                                  OnContent="Enabled" OffContent="Disabled"/>
                    <InfoBar IsOpen="True" Severity="Informational" IsClosable="False"
                             Title="Settings"
                             Message="Enabling Developer Mode exposes features currently under development. Use with caution."/>
                </StackPanel>
            </StackPanel>

        </StackPanel>
    </ScrollViewer>
</Page>