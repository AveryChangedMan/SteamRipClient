<Page
    x:Class="SteamRipApp.HelpPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SteamRipApp"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Padding="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Help &amp; Documentation" Style="{StaticResource TitleTextBlockStyle}" Margin="0,0,0,16"/>

        <AutoSuggestBox x:Name="HelpSearchBox"
                        Grid.Row="1"
                        PlaceholderText="Search help topics..."
                        QueryIcon="Find"
                        TextChanged="HelpSearchBox_TextChanged"
                        Margin="0,0,0,24"
                        Width="400"
                        HorizontalAlignment="Left"/>

        <ListView x:Name="HelpList" Grid.Row="2" SelectionMode="None">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="local:HelpItem">
                    <Grid Margin="0,0,0,16" Padding="16" Background="{ThemeResource SystemControlBackgroundChromeMediumLowBrush}" CornerRadius="8">
                        <StackPanel Spacing="8">
                            <StackPanel Orientation="Horizontal" Spacing="8">
                                <SymbolIcon Symbol="Help" Foreground="{ThemeResource SystemAccentColor}"/>
                                <TextBlock Text="{x:Bind Question}" FontWeight="Bold" FontSize="16" TextWrapping="Wrap"/>
                            </StackPanel>
                            <TextBlock Text="{x:Bind Category}" FontSize="12" Opacity="0.6" FontWeight="SemiBold"/>
                            <TextBlock Text="{x:Bind Answer}" TextWrapping="Wrap" Opacity="0.9" LineHeight="20"/>
                        </StackPanel>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <TextBlock x:Name="NoResultsText"
                   Grid.Row="2"
                   Text="No topics match your search."
                   HorizontalAlignment="Center"
                   VerticalAlignment="Center"
                   Visibility="Collapsed"
                   Opacity="0.6"/>
    </Grid>
</Page>