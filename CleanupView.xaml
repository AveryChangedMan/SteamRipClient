<Page
    x:Class="SteamRipApp.CleanupView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SteamRipApp">

    <Grid Padding="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="System Cleanup and Storage" FontSize="28" FontWeight="Bold" Margin="0,0,0,24"/>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="400"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Background="{ThemeResource SystemControlBackgroundChromeMediumLowBrush}" CornerRadius="8" Padding="20" Margin="0,0,12,0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <StackPanel Spacing="4" Margin="0,0,0,16">
                        <TextBlock Text="Redist Cleanup" FontWeight="Bold" FontSize="18"/>
                        <TextBlock Text="Remove unnecessary installers from game folders." Style="{StaticResource CaptionTextBlockStyle}" Opacity="0.7"/>
                    </StackPanel>
                    <ListView x:Name="RedistList" Grid.Row="1" SelectionMode="None">
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <Grid Padding="0,8">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <StackPanel VerticalAlignment="Center">
                                        <TextBlock Text="{Binding GameTitle}" FontWeight="SemiBold"/>
                                        <TextBlock Text="{Binding FolderPath}" FontSize="10" Opacity="0.5"/>
                                    </StackPanel>
                                    <TextBlock Grid.Column="1" Text="{Binding SizeStr}" VerticalAlignment="Center" Opacity="0.7"/>
                                </Grid>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                    <Button x:Name="RedistCleanupButton" Grid.Row="2" HorizontalAlignment="Stretch" Padding="12" Margin="0,16,0,0"
                            Style="{StaticResource AccentButtonStyle}" Click="CleanRedistBtn_Click">
                        <StackPanel Orientation="Horizontal" Spacing="8">
                            <SymbolIcon Symbol="Delete"/>
                            <TextBlock x:Name="RedistCleanupText" Text="Clean Redist (~0 MB)"/>
                        </StackPanel>
                    </Button>
                </Grid>
            </Border>

            <Border Grid.Column="1" Background="{ThemeResource SystemControlBackgroundChromeMediumLowBrush}" CornerRadius="8" Padding="20" Margin="12,0,0,0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <TextBlock Text="Library Storage Distribution" FontWeight="Bold" FontSize="18" Margin="0,0,0,16"/>
                    <Grid Grid.Row="1">
                        <Canvas x:Name="StoragePieCanvas" HorizontalAlignment="Center" VerticalAlignment="Center" Width="300" Height="300"/>
                        <StackPanel x:Name="StorageChartLegend" HorizontalAlignment="Right" VerticalAlignment="Top" Spacing="4"/>
                    </Grid>
                    <StackPanel Grid.Row="2" Spacing="8" Margin="0,20,0,0">
                        <Grid>
                            <TextBlock Text="Total Library Size:"/>
                            <TextBlock x:Name="TotalLibSizeText" Text="0 GB" HorizontalAlignment="Right" FontWeight="Bold"/>
                        </Grid>
                        <ProgressBar x:Name="StorageProgressBar" Height="10"/>
                        <Grid>
                            <TextBlock x:Name="DriveInfoText" Text="C: 0GB Free" FontSize="12" Opacity="0.7"/>
                            <TextBlock x:Name="UsedPercentageText" Text="0% Used" HorizontalAlignment="Right" FontSize="12" Opacity="0.7"/>
                        </Grid>
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</Page>